# Copy to .env — all values have sensible defaults, so .env is optional.
PORT=3000
MQTT_URL=mqtt://localhost:1883
# MQTT_USER=
# MQTT_PASS=

# How long to keep measurement history, in days.
RETENTION_DAYS=7
